# configs/detr.yaml - Simple version
dataset_path: datasets/detection
epochs: 25
batch_size: 2
learning_rate: 0.0001  # Use decimal notation instead of 1e-4
img_size: 800
project: models/detectors/detr
save_checkpoint_every: 5